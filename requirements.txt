# Update requirements.txt - BỎ --no-binary flag
cat > requirements.txt << 'EOF'
opencv-python-headless==4.10.0.84
mediapipe==0.10.14
streamlit==1.35.0
numpy==1.26.4
pandas==2.2.1
plotly==5.18.0
scikit-learn==1.3.2
scipy==1.11.4
Pillow==10.2.0
EOF

git add requirements.txt
git commit -m "Fix: upgrade to mediapipe 0.10.14 (no opencv-contrib dependency)"
git push origin main
